{
  "scripts": {
    "release-order": "spago run -m Scripts.ReleaseOrder",
    "release": "spago run -m Scripts.Release",
    "noop": "echo 'nothing to see here'"
  },
  "dependencies": {
    "xhr2": "^0.2.1"
  }
}
