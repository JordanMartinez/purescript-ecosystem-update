if has("dependencies") then .dependencies |= (
  if has("purescript-arrays") then ."purescript-arrays" |= "master" else . end |
  if has("purescript-assert") then ."purescript-assert" |= "master" else . end |
  if has("purescript-bifunctors") then ."purescript-bifunctors" |= "master" else . end |
  if has("purescript-catenable-lists") then ."purescript-catenable-lists" |= "master" else . end |
  if has("purescript-console") then ."purescript-console" |= "master" else . end |
  if has("purescript-const") then ."purescript-const" |= "master" else . end |
  if has("purescript-contravariant") then ."purescript-contravariant" |= "master" else . end |
  if has("purescript-control") then ."purescript-control" |= "master" else . end |
  if has("purescript-datetime") then ."purescript-datetime" |= "master" else . end |
  if has("purescript-distributive") then ."purescript-distributive" |= "master" else . end |
  if has("purescript-effect") then ."purescript-effect" |= "master" else . end |
  if has("purescript-either") then ."purescript-either" |= "master" else . end |
  if has("purescript-enums") then ."purescript-enums" |= "master" else . end |
  if has("purescript-exceptions") then ."purescript-exceptions" |= "master" else . end |
  if has("purescript-exists") then ."purescript-exists" |= "master" else . end |
  if has("purescript-filterable") then ."purescript-filterable" |= "master" else . end |
  if has("purescript-foldable-traversable") then ."purescript-foldable-traversable" |= "master" else . end |
  if has("purescript-foreign") then ."purescript-foreign" |= "master" else . end |
  if has("purescript-foreign-object") then ."purescript-foreign-object" |= "master" else . end |
  if has("purescript-free") then ."purescript-free" |= "master" else . end |
  if has("purescript-functions") then ."purescript-functions" |= "master" else . end |
  if has("purescript-functors") then ."purescript-functors" |= "master" else . end |
  if has("purescript-gen") then ."purescript-gen" |= "master" else . end |
  if has("purescript-graphs") then ."purescript-graphs" |= "master" else . end |
  if has("purescript-identity") then ."purescript-identity" |= "master" else . end |
  if has("purescript-integers") then ."purescript-integers" |= "master" else . end |
  if has("purescript-invariant") then ."purescript-invariant" |= "master" else . end |
  if has("purescript-lazy") then ."purescript-lazy" |= "master" else . end |
  if has("purescript-lcg") then ."purescript-lcg" |= "master" else . end |
  if has("purescript-lists") then ."purescript-lists" |= "master" else . end |
  if has("purescript-math") then ."purescript-math" |= "master" else . end |
  if has("purescript-maybe") then ."purescript-maybe" |= "master" else . end |
  if has("purescript-minibench") then ."purescript-minibench" |= "master" else . end |
  if has("purescript-newtype") then ."purescript-newtype" |= "master" else . end |
  if has("purescript-nonempty") then ."purescript-nonempty" |= "master" else . end |
  if has("purescript-numbers") then ."purescript-numbers" |= "master" else . end |
  if has("purescript-ordered-collections") then ."purescript-ordered-collections" |= "master" else . end |
  if has("purescript-orders") then ."purescript-orders" |= "master" else . end |
  if has("purescript-parallel") then ."purescript-parallel" |= "master" else . end |
  if has("purescript-partial") then ."purescript-partial" |= "master" else . end |
  if has("purescript-prelude") then ."purescript-prelude" |= "master" else . end |
  if has("purescript-profunctor") then ."purescript-profunctor" |= "master" else . end |
  if has("purescript-psci-support") then ."purescript-psci-support" |= "master" else . end |
  if has("purescript-quickcheck") then ."purescript-quickcheck" |= "master" else . end |
  if has("purescript-random") then ."purescript-random" |= "master" else . end |
  if has("purescript-record") then ."purescript-record" |= "master" else . end |
  if has("purescript-refs") then ."purescript-refs" |= "master" else . end |
  if has("purescript-safe-coerce") then ."purescript-safe-coerce" |= "master" else . end |
  if has("purescript-semirings") then ."purescript-semirings" |= "master" else . end |
  if has("purescript-st") then ."purescript-st" |= "master" else . end |
  if has("purescript-strings") then ."purescript-strings" |= "master" else . end |
  if has("purescript-tailrec") then ."purescript-tailrec" |= "master" else . end |
  if has("purescript-transformers") then ."purescript-transformers" |= "master" else . end |
  if has("purescript-tuples") then ."purescript-tuples" |= "master" else . end |
  if has("purescript-type-equality") then ."purescript-type-equality" |= "master" else . end |
  if has("purescript-typelevel-prelude") then ."purescript-typelevel-prelude" |= "master" else . end |
  if has("purescript-unfoldable") then ."purescript-unfoldable" |= "master" else . end |
  if has("purescript-unsafe-coerce") then ."purescript-unsafe-coerce" |= "master" else . end |
  if has("purescript-validation") then ."purescript-validation" |= "master" else . end |
  if has("purescript-ace") then ."purescript-ace" |= "main" else . end |
  if has("purescript-aff-bus") then ."purescript-aff-bus" |= "main" else . end |
  if has("purescript-aff-coroutines") then ."purescript-aff-coroutines" |= "main" else . end |
  if has("purescript-aff") then ."purescript-aff" |= "main" else . end |
  if has("purescript-affjax") then ."purescript-affjax" |= "main" else . end |
  if has("purescript-argonaut-codecs") then ."purescript-argonaut-codecs" |= "main" else . end |
  if has("purescript-argonaut-core") then ."purescript-argonaut-core" |= "main" else . end |
  if has("purescript-argonaut-generic") then ."purescript-argonaut-generic" |= "main" else . end |
  if has("purescript-argonaut") then ."purescript-argonaut" |= "main" else . end |
  if has("purescript-argonaut-traversals") then ."purescript-argonaut-traversals" |= "main" else . end |
  if has("purescript-arraybuffer") then ."purescript-arraybuffer" |= "main" else . end |
  if has("purescript-arraybuffer-types") then ."purescript-arraybuffer-types" |= "main" else . end |
  if has("purescript-avar") then ."purescript-avar" |= "main" else . end |
  if has("purescript-colors") then ."purescript-colors" |= "main" else . end |
  if has("purescript-concurrent-queues") then ."purescript-concurrent-queues" |= "main" else . end |
  if has("purescript-coroutines") then ."purescript-coroutines" |= "main" else . end |
  if has("purescript-css") then ."purescript-css" |= "main" else . end |
  if has("purescript-fixed-points") then ."purescript-fixed-points" |= "main" else . end |
  if has("purescript-float32") then ."purescript-float32" |= "main" else . end |
  if has("purescript-fork") then ."purescript-fork" |= "main" else . end |
  if has("purescript-formatters") then ."purescript-formatters" |= "main" else . end |
  if has("purescript-form-urlencoded") then ."purescript-form-urlencoded" |= "main" else . end |
  if has("purescript-freet") then ."purescript-freet" |= "main" else . end |
  if has("purescript-http-methods") then ."purescript-http-methods" |= "main" else . end |
  if has("purescript-js-date") then ."purescript-js-date" |= "main" else . end |
  if has("purescript-js-timers") then ."purescript-js-timers" |= "main" else . end |
  if has("purescript-js-uri") then ."purescript-js-uri" |= "main" else . end |
  if has("purescript-machines") then ."purescript-machines" |= "main" else . end |
  if has("purescript-matryoshka") then ."purescript-matryoshka" |= "main" else . end |
  if has("purescript-media-types") then ."purescript-media-types" |= "main" else . end |
  if has("purescript-now") then ."purescript-now" |= "main" else . end |
  if has("purescript-nullable") then ."purescript-nullable" |= "main" else . end |
  if has("purescript-options") then ."purescript-options" |= "main" else . end |
  if has("purescript-parsing") then ."purescript-parsing" |= "main" else . end |
  if has("purescript-pathy") then ."purescript-pathy" |= "main" else . end |
  if has("purescript-precise") then ."purescript-precise" |= "main" else . end |
  if has("purescript-profunctor-lenses") then ."purescript-profunctor-lenses" |= "main" else . end |
  if has("purescript-quickcheck-laws") then ."purescript-quickcheck-laws" |= "main" else . end |
  if has("purescript-react-dom") then ."purescript-react-dom" |= "main" else . end |
  if has("purescript-react") then ."purescript-react" |= "main" else . end |
  if has("purescript-routing") then ."purescript-routing" |= "main" else . end |
  if has("purescript-string-parsers") then ."purescript-string-parsers" |= "main" else . end |
  if has("purescript-strings-extra") then ."purescript-strings-extra" |= "main" else . end |
  if has("purescript-these") then ."purescript-these" |= "main" else . end |
  if has("purescript-uint") then ."purescript-uint" |= "main" else . end |
  if has("purescript-unicode") then ."purescript-unicode" |= "main" else . end |
  if has("purescript-unsafe-reference") then ."purescript-unsafe-reference" |= "main" else . end |
  if has("purescript-uri") then ."purescript-uri" |= "main" else . end |
  if has("purescript-canvas") then ."purescript-canvas" |= "master" else . end |
  if has("purescript-web-clipboard") then ."purescript-web-clipboard" |= "master" else . end |
  if has("purescript-web-cssom") then ."purescript-web-cssom" |= "master" else . end |
  if has("purescript-web-dom") then ."purescript-web-dom" |= "master" else . end |
  if has("purescript-web-dom-parser") then ."purescript-web-dom-parser" |= "master" else . end |
  if has("purescript-web-dom-xpath") then ."purescript-web-dom-xpath" |= "master" else . end |
  if has("purescript-web-encoding") then ."purescript-web-encoding" |= "master" else . end |
  if has("purescript-web-events") then ."purescript-web-events" |= "master" else . end |
  if has("purescript-web-fetch") then ."purescript-web-fetch" |= "master" else . end |
  if has("purescript-web-file") then ."purescript-web-file" |= "master" else . end |
  if has("purescript-web-html") then ."purescript-web-html" |= "master" else . end |
  if has("purescript-web-promise") then ."purescript-web-promise" |= "master" else . end |
  if has("purescript-web-socket") then ."purescript-web-socket" |= "master" else . end |
  if has("purescript-web-storage") then ."purescript-web-storage" |= "master" else . end |
  if has("purescript-web-streams") then ."purescript-web-streams" |= "master" else . end |
  if has("purescript-web-touchevents") then ."purescript-web-touchevents" |= "master" else . end |
  if has("purescript-web-uievents") then ."purescript-web-uievents" |= "master" else . end |
  if has("purescript-web-xhr") then ."purescript-web-xhr" |= "master" else . end |
  if has("purescript-node-buffer") then ."purescript-node-buffer" |= "master" else . end |
  if has("purescript-node-child-process") then ."purescript-node-child-process" |= "master" else . end |
  if has("purescript-node-fs-aff") then ."purescript-node-fs-aff" |= "master" else . end |
  if has("purescript-node-fs") then ."purescript-node-fs" |= "master" else . end |
  if has("purescript-node-http") then ."purescript-node-http" |= "master" else . end |
  if has("purescript-node-net") then ."purescript-node-net" |= "master" else . end |
  if has("purescript-node-path") then ."purescript-node-path" |= "master" else . end |
  if has("purescript-node-process") then ."purescript-node-process" |= "master" else . end |
  if has("purescript-node-readline") then ."purescript-node-readline" |= "master" else . end |
  if has("purescript-node-streams") then ."purescript-node-streams" |= "master" else . end |
  if has("purescript-node-url") then ."purescript-node-url" |= "master" else . end |
  if has("purescript-posix-types") then ."purescript-posix-types" |= "master" else . end |
  if has("purescript-filterable") then ."purescript-filterable" |= "main" else . end |
  if has("purescript-js-uri") then ."purescript-js-uri" |= "https://github.com/purescript-contrib/purescript-js-uri.git#main" else . end |
  if has("purescript-web-promise") then ."purescript-web-promise" |= "https://github.com/purescript-web/purescript-web-promise.git#master" else . end |
  if has("purescript-web-streams") then ."purescript-web-streams" |= "https://github.com/purescript-web/purescript-web-streams.git#master" else . end |
  .
) else . end | 
 if has ("devDependencies") then .devDependencies |= (
  if has("purescript-arrays") then ."purescript-arrays" |= "master" else . end |
  if has("purescript-assert") then ."purescript-assert" |= "master" else . end |
  if has("purescript-bifunctors") then ."purescript-bifunctors" |= "master" else . end |
  if has("purescript-catenable-lists") then ."purescript-catenable-lists" |= "master" else . end |
  if has("purescript-console") then ."purescript-console" |= "master" else . end |
  if has("purescript-const") then ."purescript-const" |= "master" else . end |
  if has("purescript-contravariant") then ."purescript-contravariant" |= "master" else . end |
  if has("purescript-control") then ."purescript-control" |= "master" else . end |
  if has("purescript-datetime") then ."purescript-datetime" |= "master" else . end |
  if has("purescript-distributive") then ."purescript-distributive" |= "master" else . end |
  if has("purescript-effect") then ."purescript-effect" |= "master" else . end |
  if has("purescript-either") then ."purescript-either" |= "master" else . end |
  if has("purescript-enums") then ."purescript-enums" |= "master" else . end |
  if has("purescript-exceptions") then ."purescript-exceptions" |= "master" else . end |
  if has("purescript-exists") then ."purescript-exists" |= "master" else . end |
  if has("purescript-filterable") then ."purescript-filterable" |= "master" else . end |
  if has("purescript-foldable-traversable") then ."purescript-foldable-traversable" |= "master" else . end |
  if has("purescript-foreign") then ."purescript-foreign" |= "master" else . end |
  if has("purescript-foreign-object") then ."purescript-foreign-object" |= "master" else . end |
  if has("purescript-free") then ."purescript-free" |= "master" else . end |
  if has("purescript-functions") then ."purescript-functions" |= "master" else . end |
  if has("purescript-functors") then ."purescript-functors" |= "master" else . end |
  if has("purescript-gen") then ."purescript-gen" |= "master" else . end |
  if has("purescript-graphs") then ."purescript-graphs" |= "master" else . end |
  if has("purescript-identity") then ."purescript-identity" |= "master" else . end |
  if has("purescript-integers") then ."purescript-integers" |= "master" else . end |
  if has("purescript-invariant") then ."purescript-invariant" |= "master" else . end |
  if has("purescript-lazy") then ."purescript-lazy" |= "master" else . end |
  if has("purescript-lcg") then ."purescript-lcg" |= "master" else . end |
  if has("purescript-lists") then ."purescript-lists" |= "master" else . end |
  if has("purescript-math") then ."purescript-math" |= "master" else . end |
  if has("purescript-maybe") then ."purescript-maybe" |= "master" else . end |
  if has("purescript-minibench") then ."purescript-minibench" |= "master" else . end |
  if has("purescript-newtype") then ."purescript-newtype" |= "master" else . end |
  if has("purescript-nonempty") then ."purescript-nonempty" |= "master" else . end |
  if has("purescript-numbers") then ."purescript-numbers" |= "master" else . end |
  if has("purescript-ordered-collections") then ."purescript-ordered-collections" |= "master" else . end |
  if has("purescript-orders") then ."purescript-orders" |= "master" else . end |
  if has("purescript-parallel") then ."purescript-parallel" |= "master" else . end |
  if has("purescript-partial") then ."purescript-partial" |= "master" else . end |
  if has("purescript-prelude") then ."purescript-prelude" |= "master" else . end |
  if has("purescript-profunctor") then ."purescript-profunctor" |= "master" else . end |
  if has("purescript-psci-support") then ."purescript-psci-support" |= "master" else . end |
  if has("purescript-quickcheck") then ."purescript-quickcheck" |= "master" else . end |
  if has("purescript-random") then ."purescript-random" |= "master" else . end |
  if has("purescript-record") then ."purescript-record" |= "master" else . end |
  if has("purescript-refs") then ."purescript-refs" |= "master" else . end |
  if has("purescript-safe-coerce") then ."purescript-safe-coerce" |= "master" else . end |
  if has("purescript-semirings") then ."purescript-semirings" |= "master" else . end |
  if has("purescript-st") then ."purescript-st" |= "master" else . end |
  if has("purescript-strings") then ."purescript-strings" |= "master" else . end |
  if has("purescript-tailrec") then ."purescript-tailrec" |= "master" else . end |
  if has("purescript-transformers") then ."purescript-transformers" |= "master" else . end |
  if has("purescript-tuples") then ."purescript-tuples" |= "master" else . end |
  if has("purescript-type-equality") then ."purescript-type-equality" |= "master" else . end |
  if has("purescript-typelevel-prelude") then ."purescript-typelevel-prelude" |= "master" else . end |
  if has("purescript-unfoldable") then ."purescript-unfoldable" |= "master" else . end |
  if has("purescript-unsafe-coerce") then ."purescript-unsafe-coerce" |= "master" else . end |
  if has("purescript-validation") then ."purescript-validation" |= "master" else . end |
  if has("purescript-ace") then ."purescript-ace" |= "main" else . end |
  if has("purescript-aff-bus") then ."purescript-aff-bus" |= "main" else . end |
  if has("purescript-aff-coroutines") then ."purescript-aff-coroutines" |= "main" else . end |
  if has("purescript-aff") then ."purescript-aff" |= "main" else . end |
  if has("purescript-affjax") then ."purescript-affjax" |= "main" else . end |
  if has("purescript-argonaut-codecs") then ."purescript-argonaut-codecs" |= "main" else . end |
  if has("purescript-argonaut-core") then ."purescript-argonaut-core" |= "main" else . end |
  if has("purescript-argonaut-generic") then ."purescript-argonaut-generic" |= "main" else . end |
  if has("purescript-argonaut") then ."purescript-argonaut" |= "main" else . end |
  if has("purescript-argonaut-traversals") then ."purescript-argonaut-traversals" |= "main" else . end |
  if has("purescript-arraybuffer") then ."purescript-arraybuffer" |= "main" else . end |
  if has("purescript-arraybuffer-types") then ."purescript-arraybuffer-types" |= "main" else . end |
  if has("purescript-avar") then ."purescript-avar" |= "main" else . end |
  if has("purescript-colors") then ."purescript-colors" |= "main" else . end |
  if has("purescript-concurrent-queues") then ."purescript-concurrent-queues" |= "main" else . end |
  if has("purescript-coroutines") then ."purescript-coroutines" |= "main" else . end |
  if has("purescript-css") then ."purescript-css" |= "main" else . end |
  if has("purescript-fixed-points") then ."purescript-fixed-points" |= "main" else . end |
  if has("purescript-float32") then ."purescript-float32" |= "main" else . end |
  if has("purescript-fork") then ."purescript-fork" |= "main" else . end |
  if has("purescript-formatters") then ."purescript-formatters" |= "main" else . end |
  if has("purescript-form-urlencoded") then ."purescript-form-urlencoded" |= "main" else . end |
  if has("purescript-freet") then ."purescript-freet" |= "main" else . end |
  if has("purescript-http-methods") then ."purescript-http-methods" |= "main" else . end |
  if has("purescript-js-date") then ."purescript-js-date" |= "main" else . end |
  if has("purescript-js-timers") then ."purescript-js-timers" |= "main" else . end |
  if has("purescript-js-uri") then ."purescript-js-uri" |= "main" else . end |
  if has("purescript-machines") then ."purescript-machines" |= "main" else . end |
  if has("purescript-matryoshka") then ."purescript-matryoshka" |= "main" else . end |
  if has("purescript-media-types") then ."purescript-media-types" |= "main" else . end |
  if has("purescript-now") then ."purescript-now" |= "main" else . end |
  if has("purescript-nullable") then ."purescript-nullable" |= "main" else . end |
  if has("purescript-options") then ."purescript-options" |= "main" else . end |
  if has("purescript-parsing") then ."purescript-parsing" |= "main" else . end |
  if has("purescript-pathy") then ."purescript-pathy" |= "main" else . end |
  if has("purescript-precise") then ."purescript-precise" |= "main" else . end |
  if has("purescript-profunctor-lenses") then ."purescript-profunctor-lenses" |= "main" else . end |
  if has("purescript-quickcheck-laws") then ."purescript-quickcheck-laws" |= "main" else . end |
  if has("purescript-react-dom") then ."purescript-react-dom" |= "main" else . end |
  if has("purescript-react") then ."purescript-react" |= "main" else . end |
  if has("purescript-routing") then ."purescript-routing" |= "main" else . end |
  if has("purescript-string-parsers") then ."purescript-string-parsers" |= "main" else . end |
  if has("purescript-strings-extra") then ."purescript-strings-extra" |= "main" else . end |
  if has("purescript-these") then ."purescript-these" |= "main" else . end |
  if has("purescript-uint") then ."purescript-uint" |= "main" else . end |
  if has("purescript-unicode") then ."purescript-unicode" |= "main" else . end |
  if has("purescript-unsafe-reference") then ."purescript-unsafe-reference" |= "main" else . end |
  if has("purescript-uri") then ."purescript-uri" |= "main" else . end |
  if has("purescript-canvas") then ."purescript-canvas" |= "master" else . end |
  if has("purescript-web-clipboard") then ."purescript-web-clipboard" |= "master" else . end |
  if has("purescript-web-cssom") then ."purescript-web-cssom" |= "master" else . end |
  if has("purescript-web-dom") then ."purescript-web-dom" |= "master" else . end |
  if has("purescript-web-dom-parser") then ."purescript-web-dom-parser" |= "master" else . end |
  if has("purescript-web-dom-xpath") then ."purescript-web-dom-xpath" |= "master" else . end |
  if has("purescript-web-encoding") then ."purescript-web-encoding" |= "master" else . end |
  if has("purescript-web-events") then ."purescript-web-events" |= "master" else . end |
  if has("purescript-web-fetch") then ."purescript-web-fetch" |= "master" else . end |
  if has("purescript-web-file") then ."purescript-web-file" |= "master" else . end |
  if has("purescript-web-html") then ."purescript-web-html" |= "master" else . end |
  if has("purescript-web-promise") then ."purescript-web-promise" |= "master" else . end |
  if has("purescript-web-socket") then ."purescript-web-socket" |= "master" else . end |
  if has("purescript-web-storage") then ."purescript-web-storage" |= "master" else . end |
  if has("purescript-web-streams") then ."purescript-web-streams" |= "master" else . end |
  if has("purescript-web-touchevents") then ."purescript-web-touchevents" |= "master" else . end |
  if has("purescript-web-uievents") then ."purescript-web-uievents" |= "master" else . end |
  if has("purescript-web-xhr") then ."purescript-web-xhr" |= "master" else . end |
  if has("purescript-node-buffer") then ."purescript-node-buffer" |= "master" else . end |
  if has("purescript-node-child-process") then ."purescript-node-child-process" |= "master" else . end |
  if has("purescript-node-fs-aff") then ."purescript-node-fs-aff" |= "master" else . end |
  if has("purescript-node-fs") then ."purescript-node-fs" |= "master" else . end |
  if has("purescript-node-http") then ."purescript-node-http" |= "master" else . end |
  if has("purescript-node-net") then ."purescript-node-net" |= "master" else . end |
  if has("purescript-node-path") then ."purescript-node-path" |= "master" else . end |
  if has("purescript-node-process") then ."purescript-node-process" |= "master" else . end |
  if has("purescript-node-readline") then ."purescript-node-readline" |= "master" else . end |
  if has("purescript-node-streams") then ."purescript-node-streams" |= "master" else . end |
  if has("purescript-node-url") then ."purescript-node-url" |= "master" else . end |
  if has("purescript-posix-types") then ."purescript-posix-types" |= "master" else . end |
  if has("purescript-filterable") then ."purescript-filterable" |= "main" else . end |
  if has("purescript-js-uri") then ."purescript-js-uri" |= "https://github.com/purescript-contrib/purescript-js-uri.git#main" else . end |
  if has("purescript-web-promise") then ."purescript-web-promise" |= "https://github.com/purescript-web/purescript-web-promise.git#master" else . end |
  if has("purescript-web-streams") then ."purescript-web-streams" |= "https://github.com/purescript-web/purescript-web-streams.git#master" else . end |
  .
) else . end
